1. Create a file on your desktop called songlist.txt
2. Inside, write the artist + song name, each pair on a separate line
3. Double click convertmp3.jar

Files are downloaded in the default location of chrome's downloads: C:\Users\<USER>\Downloads.
Requires chromedriver, for now only works with chrome.